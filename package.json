{
  "name": "trovepilot-cre",
  "private": true,
  "workspaces": ["apps/web", "workflows/trovepilot-rebalance"],
  "scripts": {
    "build": "npm run build --workspace apps/web && npm run build --workspace workflows/trovepilot-rebalance",
    "lint": "npm run lint --workspace apps/web && npm run lint --workspace workflows/trovepilot-rebalance",
    "typecheck": "npm run typecheck --workspace apps/web && npm run typecheck --workspace workflows/trovepilot-rebalance",
    "test": "npm run test --workspace apps/web && npm run test --workspace workflows/trovepilot-rebalance",
    "test:contracts": "forge test --root contracts -vv"
  },
  "engines": { "node": ">=20" }
}

